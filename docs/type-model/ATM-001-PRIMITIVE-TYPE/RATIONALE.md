# Rationale

Primitive Types are intentionally defined as abstract concepts rather than programming language types.

This separation ensures that Atlas remains technology-neutral and can be implemented consistently across different programming languages and execution environments.

Concrete language mappings are defined separately in implementation-specific standards.
